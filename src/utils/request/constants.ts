export const DEFAULT_REQUEST_TIMEOUT = 5000;

export const DEFAULT_SUCCESS_CODES = [0];

export const DEFAULT_ERROR_CODES = [9000];

export const DEFAULT_AUTH_EXPIRED_CODES = [401, 403];

export const DEFAULT_TOKEN_KEY = 'token';

export const DEFAULT_TOKEN_HEADER_KEY = 'Authorization';

export const DEFAULT_TOKEN_PREFIX = 'Bearer';

export const DEFAULT_LOGIN_URL = '/pages/login/index';

export const DEFAULT_ERROR_MESSAGE = '请求失败，请稍后重试';

export const DEFAULT_TIMEOUT_MESSAGE = '请求超时，请稍后重试';
